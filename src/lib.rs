#[cfg(test)]
mod tests {
  	#[test]
	fn test_basic() {
		assert!(1 == 1);
	}

  	// fn test_create_player() {
    // 	// arrange
		// let expected_name = String::from("Thomas");
		// let expected_points = 0;

		// // act
		// test_player = Player::new("Thomas");

		// // assert
		// assert_eq!(expected_name, test_player.name);
		// assert_eq!(expected_points, test_player.points);
  	
}